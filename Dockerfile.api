FROM node:22-alpine

WORKDIR /app

ENV NODE_ENV=production
ENV MAGERLIFE_API_HOST=0.0.0.0
ENV MAGERLIFE_API_PORT=8787

COPY package*.json ./
RUN npm ci --omit=dev

COPY src/server ./src/server

EXPOSE 8787

CMD ["npm", "run", "api:server"]
